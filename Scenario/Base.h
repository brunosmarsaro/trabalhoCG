#include "../gLib.h"

using namespace std;

class Base{

    public:
    /* Constructors and Desconstructors */
        Base();
        virtual ~Base();
    
    /* Class Methods */
        Base::create();
    
};